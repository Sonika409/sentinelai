# ── Groq (primary LLM — fast cloud inference) ─────────────────────────────
# Get a free key at https://console.groq.com
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile

# ── Ollama (offline fallback) ──────────────────────────────────────────────
# Install: https://ollama.com/download
# Pull a model: ollama pull llama3.2
# Ollama runs automatically on port 11434 — no extra config needed.
OLLAMA_MODEL=llama3.2
OLLAMA_BASE_URL=http://localhost:11434
# Other good offline models:
#   ollama pull mistral        (7B, fast, good for code)
#   ollama pull llama3.1:8b    (8B, better quality)
#   ollama pull phi3           (3.8B, very fast)
